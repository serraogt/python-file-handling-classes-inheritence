from abc import ABC
import numpy as np
#why

class Count(ABC):
    address = ''

    def __init__(self, adress):
        self.adress = adress

    def calculatefreqs(self):
        pass


class ListCount(Count):

    def calculatefreqs(self, address):
        myobj=''
        myobj = open(address)

        mylist=[]
        wordlist = []
        wordlist = myobj.split()

            for a in wordlist:
                for b in mylist:
                    if (a==b):
                        freq+=1
                        break
                        # add to world file
                    else
                        
            else:
                pass

           line.count()


class DictCount(Count):
    def calculatefreqs(self):



class main:
    asd=Count()
    asd.address=input("what is the address?")
    print (asd.adress)
